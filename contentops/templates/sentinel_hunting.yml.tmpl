id: ${id}
version: 0.1.0
asset: ${asset}
status: experimental
# Authoring lifecycle stage (T.2). See contentops/core/lifecycle_stage.py.
lifecycleStage: concept
legacy: false
metadata:
  owner: detection-engineering@example.com
  runbookUrl: https://example.com/runbooks/${id}
  severity: medium
  tactics:
    - Discovery
  techniques:
    - T1083
  expectedAlertsPerDay: 1
  fpHandling: |
    Hunting query — review results during the next hunting cadence and
    promote to an analytic rule if a stable true-positive signal emerges.
  # Section T authoring metadata. Strict mode (tenant.yml default)
  # escalates META002-005 to error; fill these before merging.
  description: |
    TODO (META002): single-paragraph summary of what this hunt surfaces.
  attackDescription: |
    TODO (META003): what attackers actually do — threat context.
  # TODO (META004): http(s):// URLs to CVE / MITRE / vendor advisory.
  references: []
  # TODO (META005): expected noise patterns when sweeping this hunt.
  falsePositives: []
  # TODO (META006, info): known evasion vectors or detection gaps.
  blindSpots: []
  # TODO (META007, info): how to act on a confirmed hunt hit.
  responseActions: []
payload:
  displayName: "${display_name}"
  description: |
    TODO: describe what this hunt surfaces and how to triage results.
  category: Hunting Queries
  tactics:
    - Discovery
  techniques:
    - T1083
  query: |
    SecurityEvent
    | where TimeGenerated > ago(1h)
    | take 100
  version: 2
